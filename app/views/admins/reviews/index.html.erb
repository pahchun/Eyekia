<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>List of all Reviews</h1>
    <ul>
      <% @items.each do |item| %>
        <li> <%= link_to item.title, item_path(item) %> </li>
          <% unless item.reviews.empty? %>
            <ul>
              <% item.reviews.each do |review| %>
                <li>
                  <%= review.rating %> - <%= review.description %>
                    <div class = "divider"/>
                     <%= button_to "Delete", admins_review_path(:id => review.id),
                      :method => :delete %>
                    </div>
                </li>
                  <ul>
                    <li> Posted By: <%= review.user.email %> </li>
                  </ul>
              <% end %>
            </ul>
          <% end %> </br>
      <% end %>
    </ul>
  </body>
</html>
